<?php

?>
<script>
    require([
        'jquery',
    ], function($) {
        require([
            'Candela_Blog/js/tag-it-custom'
        ], function() {
            $( document ).ready(function() {
                $("#posts_tags").tagit({
                    availableTags: <?= /** @noEscape */ json_encode($block->getTagsNamesArray())?>
                });
                $("#posts_tags").data("ui-tagit").tagInput.addClass("fancy");
            });
        });
    });
</script>
